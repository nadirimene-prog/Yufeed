services:
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile.dev
    command: npm run dev
    volumes:
      - ./apps/web:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
